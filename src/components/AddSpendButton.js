import React from 'react'
import { Button, Icon } from '@ui-kitten/components'

const GradientButton = () => {
  return (
    <View>
      <Button>
      <Icon style={styles.icon} fill='black' name='plus-outline' />
      </Button>
    </View>
  )
}

// const styles = StyleSheet.create({
// })

export default GradientButton
